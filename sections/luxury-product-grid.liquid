{% liquid
  assign grid_collection = section.settings.collection
  if template.name == 'collection' and grid_collection == blank
    assign grid_collection = collection
  endif
  assign grid_heading = section.settings.heading
  if grid_heading == blank and grid_collection != blank
    assign grid_heading = grid_collection.title
  endif
%}

<section class="jk-section jk-product-grid-section">
  <div class="jk-section__header jk-section__header--split">
    <div>
      {% if section.settings.eyebrow != blank %}
        <p class="jk-eyebrow">{{ section.settings.eyebrow }}</p>
      {% endif %}
      <h1 class="jk-grid-heading">{{ grid_heading | default: 'Luxury diamond jewellery' }}</h1>
      {% if section.settings.subheading != blank %}
        <p>{{ section.settings.subheading }}</p>
      {% elsif grid_collection.description != blank %}
        <p>{{ grid_collection.description | strip_html | truncate: 180 }}</p>
      {% endif %}
    </div>
    {% if grid_collection != blank %}
      <a class="jk-text-link" href="{{ grid_collection.url }}">View collection</a>
    {% endif %}
  </div>

  <div class="jk-product-grid">
    {% if grid_collection != blank and grid_collection.products_count > 0 %}
      {% for product in grid_collection.products limit: section.settings.product_limit %}
        <article class="jk-product-card">
          <a class="jk-product-card__media" href="{{ product.url }}">
            {% if product.featured_media != blank %}
              {{ product.featured_media | image_url: width: 900 | image_tag: loading: 'lazy', widths: '320, 480, 640, 900', sizes: '(min-width: 1000px) 25vw, (min-width: 640px) 50vw, 100vw', alt: product.featured_media.alt | default: product.title }}
            {% elsif section.settings.fallback_product_image != blank %}
              {% assign fallback_product_alt = section.settings.fallback_product_image_alt | default: product.title %}
              {{ section.settings.fallback_product_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '320, 480, 640, 900', sizes: '(min-width: 1000px) 25vw, (min-width: 640px) 50vw, 100vw', alt: fallback_product_alt }}
            {% else %}
              <span class="jk-product-placeholder" aria-hidden="true"></span>
            {% endif %}
          </a>
          <div class="jk-product-card__content">
            {% if product.vendor != blank %}
              <p class="jk-card-badge">{{ product.vendor }}</p>
            {% endif %}
            <h2><a href="{{ product.url }}">{{ product.title }}</a></h2>
            <p class="jk-product-card__price">
              {% if product.price_varies %}
                {{ 'products.product.from_price_html' | t: price: product.price_min | money }}
              {% else %}
                {{ product.price | money }}
              {% endif %}
            </p>
            <a class="jk-text-link" href="{{ product.url }}">{{ 'products.product.view_details' | t }}</a>
          </div>
        </article>
      {% endfor %}
    {% else %}
      {% for index in (1..4) %}
        {% liquid
          assign placeholder_image = blank
          assign placeholder_alt = blank
          case index
            when 1
              assign placeholder_image = section.settings.placeholder_image_1
              assign placeholder_alt = section.settings.placeholder_image_1_alt
            when 2
              assign placeholder_image = section.settings.placeholder_image_2
              assign placeholder_alt = section.settings.placeholder_image_2_alt
            when 3
              assign placeholder_image = section.settings.placeholder_image_3
              assign placeholder_alt = section.settings.placeholder_image_3_alt
            when 4
              assign placeholder_image = section.settings.placeholder_image_4
              assign placeholder_alt = section.settings.placeholder_image_4_alt
          endcase
          assign placeholder_alt = placeholder_alt | default: 'Joyari Krafted diamond jewellery placeholder'
        %}
        <article class="jk-product-card jk-product-card--placeholder">
          <div class="jk-product-card__media">
            {% if placeholder_image != blank %}
              {{ placeholder_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '320, 480, 640, 900', sizes: '(min-width: 1000px) 25vw, (min-width: 640px) 50vw, 100vw', alt: placeholder_alt }}
            {% else %}
              <span class="jk-product-placeholder" aria-hidden="true"></span>
            {% endif %}
          </div>
          <div class="jk-product-card__content">
            <p class="jk-card-badge">Joyari atelier</p>
            <h2>Signature diamond piece {{ index }}</h2>
            <p class="jk-product-card__price">$2,400.00</p>
            <span class="jk-text-link">Curated collection coming soon</span>
          </div>
        </article>
      {% endfor %}
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Luxury product grid",
  "tag": "section",
  "class": "shopify-section--luxury-product-grid",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Joyari edit"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Modern diamonds with timeless restraint"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "A premium edit for clients who value brilliance, ethics, and enduring design."
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "fallback_product_image",
      "label": "Fallback image for products without photos"
    },
    {
      "type": "text",
      "id": "fallback_product_image_alt",
      "label": "Fallback product image alt text",
      "default": "Joyari Krafted diamond jewellery"
    },
    {
      "type": "header",
      "content": "Placeholder card images"
    },
    {
      "type": "image_picker",
      "id": "placeholder_image_1",
      "label": "Placeholder card image 1"
    },
    {
      "type": "text",
      "id": "placeholder_image_1_alt",
      "label": "Placeholder image 1 alt text"
    },
    {
      "type": "image_picker",
      "id": "placeholder_image_2",
      "label": "Placeholder card image 2"
    },
    {
      "type": "text",
      "id": "placeholder_image_2_alt",
      "label": "Placeholder image 2 alt text"
    },
    {
      "type": "image_picker",
      "id": "placeholder_image_3",
      "label": "Placeholder card image 3"
    },
    {
      "type": "text",
      "id": "placeholder_image_3_alt",
      "label": "Placeholder image 3 alt text"
    },
    {
      "type": "image_picker",
      "id": "placeholder_image_4",
      "label": "Placeholder card image 4"
    },
    {
      "type": "text",
      "id": "placeholder_image_4_alt",
      "label": "Placeholder image 4 alt text"
    },
    {
      "type": "range",
      "id": "product_limit",
      "label": "Products to show",
      "min": 4,
      "max": 24,
      "step": 4,
      "default": 8
    }
  ],
  "presets": [
    {
      "name": "Luxury product grid"
    }
  ]
}
{% endschema %}
