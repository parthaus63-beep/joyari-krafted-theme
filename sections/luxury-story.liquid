<section class="jk-section jk-story">
  <div class="jk-story__media">
    {% if section.settings.image != blank %}
      {{ section.settings.image | image_url: width: 1300 | image_tag: loading: 'lazy', widths: '520, 720, 960, 1300', sizes: '(min-width: 900px) 45vw, 100vw', alt: section.settings.image_alt, class: 'jk-story__image' }}
    {% else %}
      {% render 'joyari-generated-image', file: 'joyari-atelier-craftsmanship.jpg', alt: section.settings.image_alt, class: 'jk-story__image' %}
    {% endif %}
  </div>

  <div class="jk-story__content">
    {% if section.settings.eyebrow != blank %}
      <p class="jk-eyebrow">{{ section.settings.eyebrow }}</p>
    {% endif %}
    <h2>{{ section.settings.heading }}</h2>
    <div class="rte">
      <p>{{ section.settings.text }}</p>
    </div>
    {% if section.settings.button_label != blank %}
      <a class="jk-text-link" href="{{ section.settings.button_link | default: routes.all_products_collection_url }}">{{ section.settings.button_label }}</a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Luxury story",
  "tag": "section",
  "class": "shopify-section--luxury-story",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Craftsmanship"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "A quieter kind of luxury"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "From the first sketch to final polish, each Joyari piece is considered for balance, wearability, and emotional permanence."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Editorial image"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image alt text",
      "default": "Joyari Krafted jewellery craftsmanship"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Discover the Joyari Standard"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Luxury story"
    }
  ]
}
{% endschema %}
